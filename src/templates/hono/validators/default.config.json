{
  "name": "default validator",
  "description": "Default validator for Hono",
  "author": "DuckyCoding",
  "placeholders": {
    "type": {
      "description": "The type of the entity",
      "value": ["export type {{Entity}} = {", "id: string;", "};"]
    },
    "createType": {
      "description": "The type of the entity used for creation operations",
      "value": "export type create{{Entity}} = Omit<{{Entity}}, 'id'>;"
    },
    "parseEntityBody": {
      "description": "The parsing of the request body",
      "value": "= await c.req.json<{{Entity}}>();"
    },
    "parsePathParams": {
      "description": "The parsing of the request path params",
      "value": "= c.req.param();"
    },
    "parsePathParamsResult": {
      "description": "The value generated by the parsing",
      "value": "pathParams"
    }
  },
  "dependencies": {},
  "devDepencencies": {}
}
